const Reader = require("./../fizzbuz/lib/utils/Reader");

describe("Unit Test for Reader class", () => {
    test('1) Create an Reader object', () => {
        // Aqui escribimos el código que queremos usar tal cuál
        // Quiero poder instanciar un objeto Spiderman con esta información:
        const Explorers = new Reader (explorers);
        
        // validamos que este codigo funcione de la forma esperada
        expect(Explorers).toBe(explorers);
     } )
})